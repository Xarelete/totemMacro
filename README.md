# Hotsite SPA — Construtora

Stack: Vite + Vue 3 + Vue Router + Tailwind CSS v4 + Vitest + Google Fonts

## Rodando
```bash
npm install
npm run dev
```
Build:
```bash
npm run build && npm run preview
```
